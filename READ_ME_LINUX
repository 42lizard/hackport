Linux build notes
=================

This tree keeps the original OpenBSD build files and adds a separate
GNU make build for Linux. On Linux, plain `make` uses `GNUmakefile`.
The original `Makefile` is still present for the OpenBSD-style `bmake`
path.

Dependencies
------------

You need a C compiler, GNU make, curses, and libbsd development headers.
On Debian or Ubuntu-like systems this usually means:

	build-essential libncurses-dev libbsd-dev

Install those with your distribution's package manager; package names may
vary.

Build
-----

Build the local binary:

	make

This first builds `makedefs`, uses it to generate `hack.onames.h` from
`def.objects.h`, and then links `./hack` against curses and libbsd.

Build the shared-install binary:

	make shared

This creates `./hack.shared` with default runtime paths suitable for a
system install.

Remove generated build output:

	make clean

Local runtime directory
-----------------------

For development runs, use a local game directory instead of system-wide
or privileged install paths:

	make hackdir

This creates `./hackdir`, copies `data`, `help`, `hh`, and `rumors`, and
creates the empty runtime files `record` and `perm`. It also creates
`hackdir/save` for saved games.

Run the game locally:

	make run

This is equivalent to:

	./hack -d ./hackdir

Score listing can be checked without starting an interactive game:

	./hack -d ./hackdir -s

Shared install
--------------

The shared install mode is intended for a local system package under
`/usr/local`. It installs:

	/usr/local/games/hack
	/usr/local/share/games/hack/data
	/usr/local/share/games/hack/help
	/usr/local/share/games/hack/hh
	/usr/local/share/games/hack/rumors
	/var/games/hack

The installed binary is setgid to the `games` group by default. Mutable
state lives under `/var/games/hack`, while text assets are read-only under
`/usr/local/share/games/hack`.

Install into the live system:

	make install-shared

Useful install variables:

	DESTDIR=...
	PREFIX=/usr/local
	GAMESDIR=/var/games/hack
	GAMEGROUP=games

For example, to stage a package image:

	make install-shared DESTDIR=/tmp/hack-install

Do not install this program setuid. The supported shared model is setgid
`games` only.

Security notes
--------------

Linux-only compatibility is collected in `linux_compat.h`. Prefer adding
small Linux shims there instead of spreading platform conditionals through
gameplay code.

Shared builds define `LINUX_SHARED` and `SECURE`. That enables checked
opens for runtime files, kernel locking for the score file, and full uid/gid
privilege dropping before child execs or user-selected directories.

Generated and runtime files
---------------------------

The following files are generated locally and should not be committed:

	hack
	hack.shared
	makedefs
	hack.onames.h
	*.o
	*.shared.o
	hackdir/
	record
	perm
	record_lock
	safelock
	save/

The top-level `record` and `perm` files may appear if you accidentally
run `./hack` without `-d ./hackdir`; remove them or leave them ignored.
